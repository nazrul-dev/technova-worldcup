// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider        = "prisma-client-js"
  binaryTargets   = ["native"]
  previewFeatures = ["referentialIntegrity"]
}

datasource db {
  provider             = "mysql"
  url                  = "mysql://u5333108_tebakjuara:Tebakjuara2022!@technova.co.id:3306/u5333108_tebakjuara"
  referentialIntegrity = "prisma"
}

model Participant {
  id           Int      @id @default(autoincrement())
  name         String
  email        String   @unique
  organization String?
  phone        String?
  prediction   String
  createdAt    DateTime @default(now())
}

model Teams {
  id    Int     @id @default(autoincrement())
  code  String?
  value String? @unique
  label String? @unique
  flag  String?
}
